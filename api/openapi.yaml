openapi: 3.1.0

info:
  title: go-http-server
  version: 0.1.0
  description: |
    Minimal Go HTTP server template demonstrating Postgres + GORM via a
    /posts CRUD surface. Stays on the standard library net/http router
    (Go 1.22+ method-prefix routing); no framework lock-in.
  license:
    name: MIT

servers:
  - url: http://localhost:3000
    description: Local development

tags:
  - name: meta
    description: Liveness + diagnostic endpoints
  - name: posts
    description: Demo CRUD entity

paths:
  /:
    get:
      tags: [meta]
      summary: Root
      description: Returns 200 with an empty body — useful as a "is anything up" probe.
      responses:
        '200':
          description: OK

  /health:
    get:
      tags: [meta]
      summary: Liveness probe
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'

  /posts:
    get:
      tags: [posts]
      summary: List all posts
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Post'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: [posts]
      summary: Create a post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePostRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Post'
        '400':
          description: |
            Validation error or constraint violation. Returned for: malformed
            JSON, missing title, missing slug, or duplicate slug.
          content:
            text/plain:
              schema:
                type: string
              examples:
                missingTitle:
                  value: title is required
                missingSlug:
                  value: slug is required
                duplicate:
                  value: post creation failed

  /posts/{slug}:
    get:
      tags: [posts]
      summary: Get a post by slug
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Post'
        '404':
          description: Not found
          content:
            text/plain:
              schema:
                type: string
                example: not found
        '500':
          $ref: '#/components/responses/InternalError'

components:
  schemas:
    Health:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: ok

    Post:
      type: object
      required: [id, slug, title, body, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
          description: Server-generated via gen_random_uuid().
          example: 43bca205-ec29-406e-abcf-62bef8806b24
        slug:
          type: string
          example: my-first-post
        title:
          type: string
          example: My first post
        body:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreatePostRequest:
      type: object
      required: [title, body, slug]
      properties:
        title:
          type: string
        body:
          type: string
        slug:
          type: string
          description: |
            Client-supplied URL slug. Unique across all (non-deleted) posts.
            Future: derived from title server-side; for now, sent by the client.

  responses:
    InternalError:
      description: Internal server error
      content:
        text/plain:
          schema:
            type: string
            example: internal error
